/**
 * 合同新增-客户信息获取
 */
import createReducer from 'UTIL/createReducer'
import { ACTION_HANDLERS } from 'ACTION/busiLease/contractAddCustom'

const initialState = []

export default createReducer(initialState, ACTION_HANDLERS)